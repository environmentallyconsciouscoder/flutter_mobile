class TransferDetails {
  String id;
  String type;
  int weight;
  DateTime timestamp;
  String dateTimeUtc;
  String hash;

  TransferDetails(this.id, this.type, this.weight, this.timestamp, this.dateTimeUtc, this.hash);
}
